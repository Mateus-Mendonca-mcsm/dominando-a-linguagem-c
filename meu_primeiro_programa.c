# include <stdio.h> // Biblioteca padrão de entrada e saída de dados

int main() {
    printf("olá, mundo!\n");
    return 0; // Retorna 0 para indicar que o programa terminou com sucesso
}



                                    //COMENTÁRIOS ÚTEIS!
/* 1- 👉 # iclude <stdio.h> // Biblioteca padrão de entrada e saída de dados.
        #include é uma diretiva de pré-processador que inclui a biblioteca padrão de entrada e saída de dados (stdio.h).
        // A biblioteca stdio.h contém funções para entrada e saída de dados, como printf e scanf.
        // A diretiva #include é usada para incluir arquivos de cabeçalho que contêm declarações de funções e macros.

   2- 👉 int main() { // Função principal do programa, onde a execução começa
        int Declara a variável inteira.
        // A variável é um espaço na memória onde podemos armazenar dados.
        main() é a função principal do programa, onde a execução começa.
        // A função main() é obrigatória em todos os programas C.
        // A função main() é o ponto de entrada do programa.
        () // Parênteses indicam que é uma função.

   3- 👉 printf("olá, mundo!\n"); // Imprime "olá, mundo!" na tela.
        // printf é uma função da biblioteca stdio.h que imprime dados na tela.
        // "olá, mundo!" é uma string que será impressa na tela.
        // \n é um caractere de nova linha que move o cursor para a próxima linha.
        // A função printf() é usada para formatar e imprimir dados na tela.
        // A função printf() pode imprimir diferentes tipos de dados, como inteiros, floats e strings.

   4- 👉 return 0; // Retorna 0 para indicar que o programa terminou com sucesso.
        // return é uma instrução que encerra a função e retorna um valor.
        // 0 indica que o programa terminou com sucesso.
        // O valor retornado pela função main() é usado pelo sistema operacional para determinar se o programa foi executado com sucesso ou não.
        // A função main() deve retornar um valor inteiro.

   5- 👉 } // Fim da função principal.
        // O caractere } indica o fim de um bloco de código.
        // Em C, os blocos de código são delimitados por chaves { e }.
        // A chave de fechamento } indica o fim da função main().
        // O código entre as chaves { e } é o corpo da função main().
        // O corpo da função main() contém as instruções que serão executadas quando o programa for executado.
*/